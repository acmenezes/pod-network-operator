
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: podnetworkconfigs.podnetwork.opdev.io
spec:
  group: podnetwork.opdev.io
  names:
    kind: PodNetworkConfig
    listKind: PodNetworkConfigList
    plural: podnetworkconfigs
    singular: podnetworkconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PodNetworkConfig is the Schema for the podnetworkconfigs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PodNetworkConfigSpec defines the desired state of PodNetworkConfig
            properties:
              eth0:
                description: CNI primary interface adjustments
                properties:
                  mtu:
                    description: for applications that need a specific MTU setting
                      other then the CNI configured MTU this will work on a per pod
                      basis not cluster wide
                    type: integer
                type: object
              links:
                description: List of new links to be configured on Pod
                items:
                  description: Link type for new Pod Interfaces
                  properties:
                    cidr:
                      description: Network Layer configurations
                      type: string
                    linkType:
                      description: Link Layer configurations
                      type: string
                    master:
                      type: string
                    mtu:
                      type: integer
                    namePrefix:
                      description: Device Info configurations
                      type: string
                    parent:
                      type: string
                  required:
                  - linkType
                  - parent
                  type: object
                type: array
              name:
                description: Name to match with pod labels
                type: string
            type: object
          status:
            description: PodNetworkConfigStatus defines the observed state of PodNetworkConfig
            properties:
              PodNetworkConfigs:
                items:
                  description: PodNetworkConfiguration verified configs for status
                  properties:
                    configList:
                      items:
                        type: string
                      type: array
                    podName:
                      type: string
                  type: object
                type: array
              phase:
                description: PodNetworkConfigPhase type for status
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
