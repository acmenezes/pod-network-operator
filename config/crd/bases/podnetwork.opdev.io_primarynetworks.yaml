
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: primarynetworks.podnetwork.opdev.io
spec:
  group: podnetwork.opdev.io
  names:
    kind: PrimaryNetwork
    listKind: PrimaryNetworkList
    plural: primarynetworks
    singular: primarynetwork
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PrimaryNetwork is the Schema for the primarynetworks API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PrimaryNetworkSpec for extra configurations on the primary
              network provided by the CNI plugin
            properties:
              HardwareAddr:
                description: A HardwareAddr represents a physical hardware address.
                format: byte
                type: string
              mtu:
                type: integer
              txQLen:
                type: integer
            type: object
          status:
            description: PrimaryNetworkStatus defines the observed state of PrimaryNetwork
            properties:
              condition:
                items:
                  properties:
                    lastHeartbeatTime:
                      type: string
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: boolean
                    type:
                      type: string
                  required:
                  - status
                  type: object
                type: array
              configurations:
                items:
                  properties:
                    currentConfigs:
                      items:
                        additionalProperties:
                          type: string
                        type: object
                      type: array
                    podName:
                      type: string
                    previousConfigs:
                      items:
                        additionalProperties:
                          type: string
                        type: object
                      type: array
                  required:
                  - previousConfigs
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
